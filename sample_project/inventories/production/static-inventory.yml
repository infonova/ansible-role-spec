---

all:
  hosts:
    foo.example.com:
  children:
    db:
      hosts:
        host1:
    webserver:
      hosts:
        host2:
          var1: "val1"
          var2: "val2"
      vars:
        ws_port: 8080
